#ifndef CRC16H
#define CRC16H
//---------------------------------------------------------------------------
#include <stdint.h>
//---------------------------------------------------------------------------
uint16_t CRC16(uint8_t data, uint16_t seed);
//---------------------------------------------------------------------------
//  Посчитать CRC16 для буфера ptr длиной size
//---------------------------------------------------------------------------
uint16_t doCRC16(void* dptr, uint32_t size);
uint16_t doCRC16(void* dptr, uint32_t size, uint16_t crc);
//---------------------------------------------------------------------------
// продолжение подсчета CRC16 с учетом предыдущего prev_crc
//---------------------------------------------------------------------------
uint16_t UpdateCRC16(uint8_t* ptr, uint32_t size, uint16_t prev_crc);
//---------------------------------------------------------------------------
#endif
